pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git credentialsId: 'jenkins-github', url: 'https://github.com/misbah-alsharif/fullStackApp.git'
            }
        }
    }
}
