pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git credentialsId: 'jenkins-github1', url: 'https://github.com/misbah-alsharif/fullStackApp.git'
            }
        }
    }
}
