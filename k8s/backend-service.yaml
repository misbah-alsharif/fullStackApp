apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  type: ClusterIP   # غيرنا من NodePort إلى ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
